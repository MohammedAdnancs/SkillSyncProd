export const dynamic = 'force-dynamic';

// This indicates to Next.js that these routes should be treated as dynamic
// and not statically pre-rendered during build time.
// This is important for routes that use client-side data fetching, authentication state, etc.
